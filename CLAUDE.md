# spark 메모앱 개발가이드

## 프로젝트 개요

### 기본 정보
- **주제**: PWA 기반 오프라인 메모장 애플리케이션
- **프로젝트명**: spark
- **배포 URL**: https://spark-zeta-ecru.vercel.app

### 목적
- 기능이 많지 않아서 직관적이고 앱실행 즉시 메모할 수 있는 앱

### 타깃 사용자
- 나만을 위한 앱

### 톤 & 비주얼 컨셉
- 깔끔하고 전문적인 느낌 

### 하지 않을 것 (Non-goals) — 잠정, 진행하며 조정 가능
- 추후 진행하며 정함

## 작업 규칙

### 1. 승인 원칙
- "시작" 또는 "go"라는 말이 나오기 전까지는 코드를 읽고 분석만 하고, 실제로 파일을 만들거나 수정하지 않을 것.
- 승인은 **세션 전체에 한 번이 아니라, 각 단계(헤더, 슬라이더, 푸터 등)마다 매번** 받을 것.
- 한 단계가 끝나면 자동으로 다음 단계로 넘어가지 말고, 반드시 새로 "시작"/"go"를 기다릴 것.
- 작업을 시작하기 전, 다음 형식으로 계획을 먼저 설명할 것.
  > "이번 단계에서는 [무엇을] 위해 [어떤 파일들]을 [어떻게] 수정하겠습니다."

### 2. 작업 기록 규칙 (이전 작업 기억하기)
- 새 세션을 시작할 때 먼저 `PROGRESS.md` 파일이 있는지 확인하고, 있으면 내용을 읽을 것.
- 가장 마지막에 기록된 작업 내용을 요약해서 사용자에게 보고할 것.
- "다음 작업 제안"이 적혀 있으면 함께 보고할 것.
- `PROGRESS.md`가 없으면 "이전 작업 기록이 없습니다"라고 알릴 것.
- `PROGRESS.md` 맨 아래에 기록을 추가할 것 (기존 기록은 지우지 말고 누적할 것).
- 작업 순서는 체크리스트를 확인하여 순서대로 진행할 것
- 진행 완료된 체크리스트에 체크 표시 할 것
- 작업 전 항상 계획을 먼저 설명하고 승인을 받을 것
- 파일 수정 시 변경 내용을 먼저 요약할 것
- 각 작업 완료 후 CLAUDE.md 체크리스트를 반드시 업데이트 할 것
- [x] 처리 시 완료 날짜를 괄호로 표기할 것. 예: - [x] 로그인 UI (2026-04-01)

## 절대 금지 사항
- 승인 없이 파일 삭제 금지
- 여러 단계를 한 번에 진행 금지
- 체크리스트에 없는 기능 임의 추가 금지
- 라이브러리 / 프레임워크 임의 설치 금지

---

## 기술 스택
- Frontend: HTML5, CSS3, Vanilla JavaScript
- PWA: Service Worker, Web App Manifest
- Storage: LocalStorage (메모 저장)
- Backup: JSON 파일 내보내기/가져오기
- 배포: Vercel
- 버전관리: GitHub

---

## 디자인 기획

### 컬러 시스템

#### Brand Colors

| 역할 | 변수명 | HEX | 용도 |
|---|---|---|---|
| Brand Navy | `--color-brand` | `#22242E` | 로고 텍스트, 주요 액션 버튼 (앱 아이콘 배경 색상) |
| Point Pink | `--color-point` | `#FF6FB0` | 별표 아이콘, 중요 메모 강조 |

#### Light Mode

| 역할 | 변수명 | HEX | 용도 |
|---|---|---|---|
| Background | `--color-bg` | `#FDF6E7` | 페이지 전체 배경, 헤더 배경 포함 (앱 아이콘 워드마크 톤) |
| Title | `--color-text-title` | `#22242E` | 제목, 강조 텍스트 |
| Body | `--color-text-body` | `#514F5C` | 본문, 일반 텍스트 |
| Placeholder | `--color-text-muted` | `#BFB5A3` | 플레이스홀더, 비활성 텍스트 |

#### Dark Mode

| 역할 | 변수명 | HEX | 용도 |
|---|---|---|---|
| Header | `--color-header-dark` | `#15161D` | 다크모드 헤더 배경 |
| Background | `--color-bg-dark` | `#101014` | 다크모드 페이지 배경 |
| Title | `--color-text-title-dark` | `#F3DFA0` | 다크모드 제목 텍스트 |
| Body | `--color-text-body-dark` | `#C7BFA8` | 다크모드 본문 텍스트 |
| Placeholder | `--color-text-muted-dark` | `#6E6A5C` | 다크모드 플레이스홀더 |
| Point Pink (dark) | `--color-point-dark` | `#FFB3C8` | 다크모드 별표 아이콘 |

#### CSS 변수 선언 규칙
- 모든 CSS 변수는 `css/style.css` 상단의 `:root {}` 블록에만 선언
- 다크모드 변수는 `[data-theme="dark"] {}` 셀렉터로 관리
- 변수명 임의 변경 금지

### 타이포그래피
- **기본 폰트**: Pretendard
- **폰트 파일 위치**: `/public/fonts/pretendard/` (woff2, web/static 서브셋)
- **사용 웨이트**: 400 Regular · 500 Medium · 600 SemiBold · 700 Bold
- **font-family 선언**: `'Pretendard', -apple-system, BlinkMacSystemFont, sans-serif`

### 아이콘
- **라이브러리**: lucide-react

### UI 레이아웃 규칙
- **다크모드 토글**: 더보기 메뉴(⋮) 내부에 배치
- **글자크기 조절**: 더보기 메뉴(⋮) 내부에 배치

## 파일 구조
> ℹ️ 폴더 단위 역할만 표기. 개별 파일은 프로젝트 진행 중 실제 생성되는 대로 늘어나므로 나열하지 않음.

```
[프로젝트명]/
├── index.html          → 앱 엔트리 포인트
├── css/                  → 스타일시트
├── js/                   → 로직
└── [필요한 폴더 추가]
```

---

## 코딩 규칙

### HTML
- 시맨틱 태그 우선 사용 (header, nav, main, article, section, footer)
- 주석은 한국어로 작성
- 들여쓰기는 2칸

### CSS
- 클래스명은 명확한 의미 전달
- Vanilla CSS 사용 (프레임워크 사용 금지)
- 주석으로 섹션 구분
- 컬러 시스템은 CSS 변수로 관리 (변수명 임의 변경 금지)

### JavaScript
- ES6+ 문법 사용
- 가독성을 위해 함수는 작게 분리
- 주석은 한국어로 작성

---

## 메모 데이터 구조

모든 메모는 아래 구조로 저장한다. **2단계부터 이 구조를 그대로 사용할 것.**

```json
{
  "id": "uuid",
  "content": "메모 내용",
  "folder": null,
  "starred": false,
  "images": [],
  "createdAt": "2026-04-01T00:00:00.000Z",
  "updatedAt": "2026-04-01T00:00:00.000Z"
}
```

| 필드 | 타입 | 설명 |
|------|------|------|
| `id` | string | crypto.randomUUID() 로 생성 |
| `content` | string | 메모 본문 전체 |
| `folder` | string \| null | 폴더명 (없으면 null = 미분류) |
| `starred` | boolean | 중요 메모 여부 |
| `images` | array | Base64 이미지 배열 (초기값 []) |
| `createdAt` | ISO8601 | 생성 시각 |
| `updatedAt` | ISO8601 | 마지막 수정 시각 |

---

## 전체 진행 상황
- [x] 1단계: 프로젝트 기본 설정 (2026-07-17)
- [x] 2단계: 메모앱 기본 기능 CRUD (2026-07-17)
- [ ] 3단계: UI 확장 (상세보기 / 더보기 메뉴 / 다크모드 / 글자크기)
- [ ] 4단계: PWA 기능 (Manifest / Service Worker / 오프라인)
- [ ] 5단계: 데이터 관리 (백업·복원 / 이미지 첨부)
- [ ] 6단계: 고급 PWA 기능 (설치 배너 / 햅틱 / Wake Lock)
- [ ] 7단계: 배포 및 테스트
- [ ] 8단계: 추가기능 — 폴더 관리
- [ ] 9단계: 추가기능 — 음성 메모

---

## 1단계. 프로젝트 기본 설정

### 파일 구조 생성
- [x] `index.html` 파일 생성 (2026-07-17)
- [x] `css/style.css` 파일 생성 (`:root` CSS 변수 포함) (2026-07-17)
- [x] `js/app.js` 파일 생성 (2026-07-17)
- [x] `manifest.json` 파일 생성 (2026-07-17)
- [x] `icons/icon-192x192.png` 배치 확인 (2026-07-17)
- [x] `icons/icon-512x512.png` 배치 확인 (2026-07-17)

### Git 설정
- [x] GitHub 저장소 생성 (2026-07-17)
- [x] `.gitignore` 파일 생성 (2026-07-17)
- [x] 첫 커밋 완료 (2026-07-17)
- [x] 원격 저장소 연결 완료 (2026-07-17)

---

## 2단계. 메모앱 기본 기능 (CRUD)

> ⚠️ 메모 저장 시 반드시 위의 **메모 데이터 구조**를 사용할 것
> `folder: null`, `images: []` 포함하여 처음부터 완전한 구조로 저장

### 메인화면 디자인 초안 잡기
- [x] 메모 입력 영역 (textarea) (2026-07-17)
- [x] 메모 저장 버튼 (2026-07-17)
- [x] 중요 메모 토글 버튼 (별 버튼) (2026-07-17)
- [x] 메모 목록 보기 (2026-07-17)
- [x] 검색 입력창 (2026-07-17)
- [x] 중요 메모 필터 버튼 (토글) (2026-07-17)
- [x] 기본 스타일링 (CSS) (2026-07-17)

### 배포하기 (초안 확인용)
> ℹ️ 메인화면 디자인은 앱 전체 톤을 결정하므로, 실기기(폰)에서 바로 확인하기 위해 정식 배포(7단계)보다 먼저 가볍게 배포한다.
- [x] GitHub 저장소에 푸시 (2026-07-17)
- [x] Vercel 배포 (2026-07-17)
- [x] 폰에서 디자인 확인 (2026-07-17)

### 메모 저장/불러오기
- [x] LocalStorage에 메모 저장 (브라우저 종료 후에도 유지) (2026-07-17)
- [x] 저장된 메모 불러오기 (2026-07-17)
- [x] 각 메모마다 고유 ID 부여 (`crypto.randomUUID()`) (2026-07-17)
싲
### CRUD 기능
- [x] **C**reate: 새 메모 추가 (2026-07-17)
- [x] **R**ead: 메모 목록 보기 (2026-07-17)
- [x] **U**pdate: 메모 수정 (`updatedAt` 갱신 포함) (2026-07-17)
- [x] **D**elete: 메모 삭제 (2026-07-17)

### 검색 & 필터
- [x] 실시간 키워드 검색 (2026-07-17)
- [x] 중요 메모만 보기 필터 (2026-07-17)

---

## 3단계. UI 확장

### 상세보기 화면
- [ ] 메모 클릭 시 상세보기 열기
- [ ] 수정 버튼
- [ ] 삭제 버튼
- [ ] 공유 버튼 (Web Share API)
- [ ] 복사 버튼 (Clipboard API)
- [ ] 뒤로가기 버튼

### 더보기 메뉴 (⋮)
- [ ] 더보기 메뉴 UI 및 열기/닫기
- [ ] 메모 내보내기 버튼 연결
- [ ] 메모 가져오기 버튼 연결
- [ ] 글자크기 조절 (매우작게 / 작게 / 보통 / 크게 / 매우크게, 5단계) + LocalStorage 저장
- [ ] 다크모드 토글 버튼 (더보기 메뉴 내부) + LocalStorage 저장

---

## 4단계. PWA 기능

### Web App Manifest
- [ ] `manifest.json` 작성 (앱 이름, 아이콘, 테마 색상, 시작 URL)
- [ ] `index.html`에 manifest 연결

### Service Worker
- [ ] `sw.js` 생성 — 캐시 파일 목록 정의
- [ ] `skipWaiting()` + `clients.claim()` 적용 — 배포 후 앱 재실행 시 최신 버전 즉시 반영
- [ ] 오프라인에서 캐시된 파일 제공
- [ ] `js/sw-register.js` 생성 (Service Worker 등록)

### 오프라인 동작 테스트
- [ ] Service Worker "activated and is running" 확인
- [ ] 오프라인 모드에서 메모 추가 / 수정 / 삭제 테스트
- [ ] 온라인 복구 후 데이터 유지 확인

---

## 5단계. 데이터 관리

### 백업 — 메모 내보내기 (Export)
- [ ] LocalStorage 메모를 JSON 파일로 다운로드
- [ ] 파일명에 날짜 포함

### 복원 — 메모 가져오기 (Import)
- [ ] JSON 파일 선택 및 읽기
- [ ] 기존 데이터 유지하며 추가 (ID 중복 제거)
- [ ] 성공/실패 토스트 알림
- [ ] 잘못된 파일 형식 검증

### 이미지 첨부
- [ ] 이미지 추가 버튼 (갤러리 / 카메라 선택 메뉴)
- [ ] FileReader로 이미지 → Base64 변환
- [ ] **이미지 용량 제한: 1장당 최대 500KB** (초과 시 토스트 경고)
- [ ] **저장 전 LocalStorage 잔여 용량 체크** (부족 시 경고)
- [ ] 에디터 이미지 미리보기 + 삭제 버튼
- [ ] LocalStorage에 Base64로 저장 (`images` 배열에 추가)
- [ ] 상세보기 화면에 첨부 이미지 표시

---

## 6단계. 고급 PWA 기능

### 앱 설치 유도 배너
- [ ] beforeinstallprompt 이벤트 캐치
- [ ] 헤더에 설치 버튼 표시 (설치 가능할 때만)
- [ ] 설치 버튼 클릭 시 설치 프롬프트 실행
- [ ] 설치 완료 후 버튼 자동 숨김

### 햅틱 피드백 (Vibration API)
- [ ] 메모 저장 시 진동 1회 (100ms)
- [ ] 메모 삭제 시 진동 2회 (100-50-100ms)

### 화면 꺼짐 방지 (Screen Wake Lock API)
- [ ] 입력창 focus 시 Wake Lock ON
- [ ] 저장 / blur 시 Wake Lock OFF
- [ ] 앱 백그라운드 복귀 시 Wake Lock 재요청

---

## 7단계. 배포 및 테스트

### Vercel 배포
- [x] GitHub 저장소에 최종 코드 푸시 (2026-07-17)
- [x] Vercel 프로젝트 생성 및 GitHub 연결 (2026-07-17)
- [x] 배포 완료 — URL: https://spark-zeta-ecru.vercel.app (2026-07-17)

### PWA 설치 테스트
- [ ] 모바일 브라우저에서 접속
- [ ] 홈 화면에 추가 및 아이콘 확인
- [ ] 설치 후 CRUD / 오프라인 / 전체 기능 정상 작동 확인

---

## 8단계. 추가기능 — 폴더 관리

> ℹ️ 메모 데이터 구조에 `folder` 필드가 2단계부터 포함되어 있으므로
> 이 단계는 UI 구현에만 집중하면 됨. 데이터 마이그레이션 불필요.

### 폴더 자동 생성
- [ ] 메모 첫 줄이 `폴더명/` 형식이면 `folder` 속성 자동 추출
- [ ] 같은 폴더명을 가진 메모끼리 자동 그룹화
- [ ] 폴더명 줄은 메모 목록 미리보기에서 숨김 처리

### 폴더 UI
- [ ] 메모 목록 상단에 폴더 탭/버튼 목록 자동 생성
- [ ] "전체" 탭 (기본값) + 폴더별 탭 표시
- [ ] 폴더 없는 메모는 "미분류"로 표시
- [ ] 폴더 탭 클릭 시 해당 메모만 필터링

### 폴더 삭제
- [ ] 폴더 탭 길게 누르면 삭제 옵션 표시
- [ ] 폴더 삭제 시 메모는 미분류로 이동 (`folder: null` 처리, 메모 삭제 안 함)
- [ ] 해당 폴더의 모든 메모 일괄 업데이트 (`updatedAt` 갱신)

---

## 9단계. 추가기능 — 음성 메모

> ℹ️ 방향 결정 필요: 아래 두 가지 중 하나를 선택하거나 둘 다 구현
> - A안: 음성을 텍스트로 변환해서 메모 내용에 입력 (저장 용량 부담 없음)
> - B안: 음성 파일 자체를 녹음해서 첨부 (원본 목소리 보존, 용량 큼)
> 시작 전 세션에서 A/B 여부를 먼저 확인받을 것

### 마이크 버튼 UI
- [ ] 마이크 아이콘 클릭 시 녹음 시작 / 재클릭 시 종료
- [ ] 녹음 중 상태 표시 (아이콘 색상 변경 또는 파형 애니메이션)
- [ ] 녹음 중 취소 버튼

### A안. 음성 → 텍스트 변환 (Web Speech API)
- [ ] Web Speech API 지원 여부 브라우저 체크
- [ ] 미지원 브라우저 대응 (버튼 비활성화 + 안내 문구)
- [ ] 인식된 텍스트를 커서 위치에 실시간 삽입
- [ ] 언어 설정 (한국어 `ko-KR` 기본값)
- [ ] 인식 오류 시 토스트 알림

### B안. 음성 파일 녹음 저장 (MediaRecorder API)
- [ ] 마이크 권한 요청 및 거부 시 안내 처리
- [ ] MediaRecorder로 녹음 → Blob 생성
- [ ] Blob → Base64 변환
- [ ] **음성 파일 용량 제한: 1건당 최대 [ ]MB** (초과 시 토스트 경고)
- [ ] 저장 전 LocalStorage 잔여 용량 체크
- [ ] 메모 데이터 구조에 `audio` 필드 추가 여부 결정 (예: `audio: null | base64string`)
- [ ] 상세보기 화면에 오디오 플레이어(재생/일시정지) 표시
- [ ] 음성 메모 삭제 버튼

### 공통
- [ ] iOS Safari 등 지원 제한 브라우저 대응 확인
- [ ] 음성 메모 관련 아이콘/버튼 다크모드 스타일 적용
